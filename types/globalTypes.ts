export interface IAssetData {
  symbol: string
  name: string
  decimals: number
  contractAddress: string
  balance?: string
}

export interface ISoscialMediaImage {
  url: string
  img: string,
  width: number,
  height: number
}